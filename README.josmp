To build and run xv6 on josmp:
$ make HW=josmp rsync setup-xv6
then ssh josmp and reboot.

If josmp is already running xv6, then
$ make HW=josmp rsync reboot-xv6

To restore booting into local Linux
$ make HW=josmp reboot-linux
